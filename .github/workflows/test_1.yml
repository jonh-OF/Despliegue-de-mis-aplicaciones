name: despliegue

on: 
  push:
    branches:
      - main
      
  pull_request:
    branches:
      -main


jobs:
  deploy:
    runs-on: ubuntu-latest
   



   
  

    steps:
    - name: Checkout
      uses: actions/checkout@v5.0.0
    
    - name: instalacion
      run: |
        chmod +x insta.sh
        ./insta.sh


    - name: action
      user: ./.github/actions/my-action
